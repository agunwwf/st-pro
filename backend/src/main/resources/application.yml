spring:
  ai:
    openai:
      api-key: sk-9a7440e6433049ceb91abe9ed108def6
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
  datasource:
    url: jdbc:mysql://localhost:3306/apple_admin?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 40MB
  sql:
    init:
      schema-locations: classpath:db/mysql_schema.sql
      mode: always

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

server:
  tomcat:
    max-swallow-size: 20MB
    max-http-form-post-size: 40MB

jwt:
  secret: asdfgHJHJKL123@#$$%789